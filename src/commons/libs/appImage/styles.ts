import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 86,
    height: 86,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export {styles};
