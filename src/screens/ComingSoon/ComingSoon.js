import React from 'react';
import {AppContainer, AppText} from '../../commons/libs';

export function ComingSoon() {
  return (
    <AppContainer style={{alignItems: 'center', justifyContent: 'center'}}>
      <AppText>ComingSoon</AppText>
    </AppContainer>
  );
}
